language: rust
cache: cargo
rust:
  - stable
before_script:
  - rustup component add rustfmt
script:
  cargo fmt -- --check &&
  cargo build --verbose &&
  cargo test  --verbose &&
  cargo doc   --verbose
dist: xenial
